# Employee File Upload

This Go application allows users to upload an Excel file containing employee data, which is stored in a MySQL database. The application also provides a RESTful API for managing employee records.

## Getting Started

### Prerequisites

- Go (1.20 or later)
- Docker
- Docker Compose

### Installation

1. Clone the repository:
   ```bash
   git clone <repository-url>
   cd employee-file-upload
